<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permessi base -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Foreground service generico -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Foreground service specifico audio/video (Media Playback) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Foreground service con accesso al microfono (Android 14 richiede permessi aggiuntivi) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- Permesso necessario per accedere al microfono -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- ⚠️ Permessi aggiuntivi richiesti da Android 14 per FGS MIC -->
    <!-- Almeno uno tra questi doveva esserci: ne aggiungiamo due, totalmente sicuri -->
    <uses-permission android:name="android.permission.CAPTURE_AUDIO_OUTPUT" />
    <uses-permission android:name="android.permission.CAPTURE_VOICE_COMMUNICATION_OUTPUT" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Servizio FOREGROUND per VoiceGuide AirLink -->
        <service
            android:name=".foreground.VoiceGuideForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone" />
    </application>

</manifest>
